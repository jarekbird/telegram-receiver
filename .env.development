# ==============================================================================
# Development Environment Variables Configuration
# ==============================================================================
# This file contains development-specific environment variable values.
# Used during local development with safe, non-production values suitable
# for development work.
#
# Note: Never commit actual secrets to version control. Replace placeholder
# values (especially secrets and tokens) with your own development credentials.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Environment
# ------------------------------------------------------------------------------
# Node.js environment (development, test, production)
# Controls various behaviors like error reporting and logging verbosity
NODE_ENV=development

# Server port number (default: 3000)
# The port on which the Express server will listen for incoming requests
PORT=3000

# Logging level (info, debug, warn, error)
# Controls the verbosity of application logs
# Set to debug for more verbose logging during development
LOG_LEVEL=debug

# ------------------------------------------------------------------------------
# Telegram Configuration
# ------------------------------------------------------------------------------
# Telegram Bot API token from BotFather
# Replace with your development bot token
TELEGRAM_BOT_TOKEN=your_development_bot_token_here

# Secret token for webhook authentication
# Replace with your development webhook secret
TELEGRAM_WEBHOOK_SECRET=your_development_webhook_secret_here

# Base URL for Telegram webhook registration
# Uses localhost for local development
TELEGRAM_WEBHOOK_BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Cursor Runner Configuration
# ------------------------------------------------------------------------------
# URL of the Cursor Runner service
# Uses localhost for local development
CURSOR_RUNNER_URL=http://localhost:3001

# Request timeout in seconds (default: 300)
CURSOR_RUNNER_TIMEOUT=300

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# Redis connection URL
# Uses local Redis instance for local development
REDIS_URL=redis://localhost:6379

# ------------------------------------------------------------------------------
# ElevenLabs Configuration
# ------------------------------------------------------------------------------
# API key for ElevenLabs services (optional, for audio features)
# Replace with your development API key if using audio features
ELEVENLABS_API_KEY=your_development_elevenlabs_api_key_here

# ------------------------------------------------------------------------------
# Webhook Secret
# ------------------------------------------------------------------------------
# Admin secret for management endpoints
# Replace with your development webhook secret
WEBHOOK_SECRET=your_development_webhook_secret_here
