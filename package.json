{
  "name": "telegram-receiver",
  "version": "1.0.0",
  "description": "Telegram webhook receiver that forwards messages to Cursor Runner API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "nodemon --exec ts-node src/index.ts",
    "start": "node dist/index.js",
    "lint": "echo 'Linting not configured yet' && exit 0",
    "lint:fix": "echo 'Lint fix not configured yet' && exit 0",
    "format": "echo 'Formatting not configured yet' && exit 0",
    "format:check": "echo 'Format check not configured yet' && exit 0",
    "test": "npx jest",
    "test:coverage": "npx jest --coverage"
  },
  "keywords": [
    "telegram",
    "webhook",
    "bot",
    "cursor-runner",
    "typescript",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/jest": "^29.5.14",
    "@types/node": "20.19.25",
    "@types/supertest": "^2.0.16",
    "jest": "^29.7.0",
    "nodemon": "3.1.11",
    "supertest": "^6.3.4",
    "ts-jest": "^29.4.5",
    "ts-node": "10.9.2",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0"
  }
}
