# ==============================================================================
# Environment Variables Configuration
# ==============================================================================
# This file contains all environment variables used by the Telegram Receiver
# application. Copy this file to .env.development, .env.test, or .env.production
# and update the values according to your environment.
#
# Note: Never commit actual secrets to version control. Use placeholder values
# in this example file and set real values in your local .env files or via
# deployment configuration.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Application name displayed in logs and API responses
APP_NAME=Virtual Assistant API

# Application version (semantic versioning: major.minor.patch)
APP_VERSION=1.0.0

# Node.js environment (development, test, production)
# Controls various behaviors like error reporting and logging verbosity
NODE_ENV=development

# Server port number (default: 3000)
# The port on which the Express server will listen for incoming requests
PORT=3000

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Logging level (info, debug, warn, error)
# Controls the verbosity of application logs
# - info: Standard operational messages
# - debug: Detailed diagnostic information
# - warn: Warning messages for potentially harmful situations
# - error: Error messages for failures
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Telegram Bot Configuration
# ------------------------------------------------------------------------------
# Telegram Bot API token obtained from BotFather
# Required for authenticating with Telegram Bot API
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Secret token for webhook authentication
# Used to verify that webhook requests are coming from Telegram
# Set this in Telegram webhook configuration and use it to validate requests
TELEGRAM_WEBHOOK_SECRET=your_webhook_secret_token_here

# Base URL for Telegram webhook callbacks
# The public URL where Telegram will send webhook updates
# Example: https://your-domain.com or http://localhost:3000 for development
TELEGRAM_WEBHOOK_BASE_URL=https://your-domain.com

# ------------------------------------------------------------------------------
# Cursor Runner Configuration
# ------------------------------------------------------------------------------
# URL of the cursor-runner service
# The base URL where cursor-runner API is accessible
# In Docker: http://cursor-runner:3001
# Local development: http://localhost:3001
CURSOR_RUNNER_URL=http://cursor-runner:3001

# Timeout for cursor-runner requests in seconds (default: 300)
# Maximum time to wait for a response from cursor-runner before timing out
CURSOR_RUNNER_TIMEOUT=300

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# Redis connection URL
# Used for storing pending request state and job queues
# Format: redis://[host]:[port]/[database]
# In Docker: redis://redis:6379/0
# Local development: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# ElevenLabs Configuration (Optional - for audio features)
# ------------------------------------------------------------------------------
# ElevenLabs API key for speech-to-text and text-to-speech features
# Required only if using audio transcription or text-to-speech functionality
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ElevenLabs speech-to-text model ID
# Model used for transcribing audio messages to text
# Examples: 'scribe_v1', 'scribe_v1_experimental', 'scribe_v2'
# Optional - only required if using audio transcription
ELEVENLABS_STT_MODEL_ID=

# ElevenLabs text-to-speech model ID
# Model used for converting text responses to speech
# Examples: 'eleven_turbo_v2_5', 'eleven_multilingual_v2'
# Optional - only required if using text-to-speech responses
ELEVENLABS_TTS_MODEL_ID=

# ElevenLabs voice ID for text-to-speech
# The voice to use when converting text to speech
# Optional - only required if using text-to-speech responses
ELEVENLABS_VOICE_ID=

# ------------------------------------------------------------------------------
# Admin Configuration
# ------------------------------------------------------------------------------
# Admin webhook secret for protecting admin endpoints
# Used to authenticate requests to admin-only endpoints
# Set a strong, random value in production
WEBHOOK_SECRET=changeme

# ------------------------------------------------------------------------------
# Notes Repository Configuration (Optional)
# ------------------------------------------------------------------------------
# Default repository for note operations
# Used by note writer tools to determine where to save notes
# Optional - can be overridden per request
DEFAULT_NOTES_REPOSITORY=
