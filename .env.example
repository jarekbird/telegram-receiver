# ==============================================================================
# Environment Variables Configuration Template
# ==============================================================================
# This file serves as a template for environment variable configuration.
# Copy this file to .env.development, .env.test, or .env.production and
# update the values according to your environment.
#
# Note: Never commit actual secrets to version control. Use placeholder values
# in this example file and set real values in your local .env files or via
# deployment configuration.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Environment
# ------------------------------------------------------------------------------
# Node.js environment (development, test, production)
# Controls various behaviors like error reporting and logging verbosity
NODE_ENV=development

# Server host address (default: 0.0.0.0)
# The host address on which the Express server will listen for incoming requests
HOST=0.0.0.0

# Server port number (default: 3000)
# The port on which the Express server will listen for incoming requests
PORT=3000

# Logging level (info, debug, warn, error)
# Controls the verbosity of application logs
LOG_LEVEL=info

# ------------------------------------------------------------------------------
# Application Metadata
# ------------------------------------------------------------------------------
# Application name (default: "Virtual Assistant API")
# Used in health checks and API responses
APP_NAME=Virtual Assistant API

# Application version (default: "1.0.0")
# Used in health checks and API responses
APP_VERSION=1.0.0

# ------------------------------------------------------------------------------
# Telegram Bot Configuration
# ------------------------------------------------------------------------------
# Telegram Bot API token from BotFather
# Required for Telegram bot functionality
# Get your token from: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Secret token for Telegram webhook authentication
# Used to verify webhook requests from Telegram
# Default: changeme (change this in production!)
TELEGRAM_WEBHOOK_SECRET=changeme

# Base URL for Telegram webhook registration
# The public URL where your webhook endpoint will be accessible
# For local development, use localhost with port
TELEGRAM_WEBHOOK_BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Cursor Runner Configuration
# ------------------------------------------------------------------------------
# URL of the cursor-runner service
# Used to communicate with the cursor-runner API
# For local development, use localhost
CURSOR_RUNNER_URL=http://localhost:3001

# Request timeout in seconds (default: 300)
# Timeout for cursor-runner API requests
CURSOR_RUNNER_TIMEOUT=300

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# Redis connection URL
# Used for job queues and caching
# Format: redis://[host]:[port]/[database]
REDIS_URL=redis://localhost:6379/0

# ------------------------------------------------------------------------------
# ElevenLabs Configuration (Optional)
# ------------------------------------------------------------------------------
# API key for ElevenLabs services (optional, for audio features)
# Required for speech-to-text and text-to-speech functionality
# Get your API key from: https://elevenlabs.io/
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Speech-to-text model ID
# ElevenLabs model identifier for speech-to-text conversion
# Examples: 'scribe_v1', 'scribe_v1_experimental', 'scribe_v2'
ELEVENLABS_STT_MODEL_ID=scribe_v1

# Text-to-speech model ID
# ElevenLabs model identifier for text-to-speech conversion
# Examples: 'eleven_turbo_v2_5', 'eleven_multilingual_v2'
ELEVENLABS_TTS_MODEL_ID=eleven_turbo_v2_5

# Voice ID for text-to-speech
# ElevenLabs voice identifier for TTS voice selection
# Get voice IDs from: https://elevenlabs.io/app/voices
ELEVENLABS_VOICE_ID=your_elevenlabs_voice_id_here

# ------------------------------------------------------------------------------
# Webhook Secret
# ------------------------------------------------------------------------------
# Admin webhook secret for management endpoints
# Used to authenticate admin webhook requests
# Default: changeme (change this in production!)
WEBHOOK_SECRET=changeme

# ------------------------------------------------------------------------------
# CORS Configuration (Optional)
# ------------------------------------------------------------------------------
# Enable CORS (Cross-Origin Resource Sharing) for cross-origin requests
# Set to 'true' to enable CORS, 'false' or leave unset to disable (default: disabled)
# CORS is disabled by default to match Rails behavior where CORS is commented out
# Enable CORS if the API will be called from web browsers or frontend applications
CORS_ENABLED=false

# Allowed origins for CORS requests
# Single origin: CORS_ORIGIN=https://example.com
# Multiple origins (comma-separated): CORS_ORIGIN=https://example.com,https://app.example.com
# All origins (development only, not recommended for production): CORS_ORIGIN=*
# Default: * (only used if CORS_ENABLED=true)
CORS_ORIGIN=*

# ------------------------------------------------------------------------------
# Notes Repository Configuration (Optional)
# ------------------------------------------------------------------------------
# Default repository for note operations (optional)
# Path to the default repository used for note writing operations
# Leave empty if not using default repository
DEFAULT_NOTES_REPOSITORY=
