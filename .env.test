# ==============================================================================
# Test Environment Configuration
# ------------------------------------------------------------------------------
# Environment variables for testing environment
# Uses test tokens, shorter timeouts, and test database
# This file is used during test execution and should contain values suitable
# for automated testing, including minimal logging, test-specific ports, and
# safe placeholder values for secrets.
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Environment
# ------------------------------------------------------------------------------
# Node.js environment (test)
# Controls various behaviors like error reporting and logging verbosity
NODE_ENV=test

# Server host address (default: 0.0.0.0)
# The host address on which the Express server will listen for incoming requests
HOST=0.0.0.0

# Server port number (test-specific port to avoid conflicts with development)
# The port on which the Express server will listen for incoming requests
PORT=3001

# Logging level (error for minimal logging in tests)
# Controls the verbosity of application logs
# Set to error to reduce test output noise
LOG_LEVEL=error

# ------------------------------------------------------------------------------
# Telegram Configuration
# ------------------------------------------------------------------------------
# Telegram Bot API token (test placeholder - not a real token)
TELEGRAM_BOT_TOKEN=test_bot_token

# Secret token for webhook authentication (test placeholder)
TELEGRAM_WEBHOOK_SECRET=test_webhook_secret

# Base URL for Telegram webhook registration (test-specific URL)
# Uses localhost with test port
TELEGRAM_WEBHOOK_BASE_URL=http://localhost:3001

# ------------------------------------------------------------------------------
# Cursor Runner Configuration
# ------------------------------------------------------------------------------
# URL of the Cursor Runner service (test-specific URL)
# Uses localhost for test environment
CURSOR_RUNNER_URL=http://localhost:3001

# Request timeout in seconds (shorter timeout for faster test execution)
CURSOR_RUNNER_TIMEOUT=30

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# Redis connection URL (test-specific database, isolated from development)
# Uses a separate Redis database (1) for tests to avoid conflicts
REDIS_URL=redis://localhost:6379/1

# ------------------------------------------------------------------------------
# ElevenLabs Configuration
# ------------------------------------------------------------------------------
# API key for ElevenLabs services (empty - no real API key needed for tests)
ELEVENLABS_API_KEY=

# Speech-to-text model ID (test placeholder)
ELEVENLABS_STT_MODEL_ID=scribe_v1

# Text-to-speech model ID (test placeholder)
ELEVENLABS_TTS_MODEL_ID=eleven_turbo_v2_5

# Voice ID for text-to-speech (test placeholder)
ELEVENLABS_VOICE_ID=test_voice_id

# ------------------------------------------------------------------------------
# Application Metadata
# ------------------------------------------------------------------------------
# Application name (test-specific)
APP_NAME=Virtual Assistant API Test

# Application version (test-specific)
APP_VERSION=1.0.0-test

# ------------------------------------------------------------------------------
# Notes Repository Configuration (Optional)
# ------------------------------------------------------------------------------
# Default repository for note operations (optional, empty for tests)
DEFAULT_NOTES_REPOSITORY=

# ------------------------------------------------------------------------------
# Webhook Secret
# ------------------------------------------------------------------------------
# Admin secret for management endpoints (test placeholder)
WEBHOOK_SECRET=test_webhook_secret
